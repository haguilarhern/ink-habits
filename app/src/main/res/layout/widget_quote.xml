<?xml version="1.0" encoding="utf-8"?>
<!-- Quote-only widget: mirrors the app's current quote (typed text or handwriting).
     Tapping it opens the in-app quote editor. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/quoteBody"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:padding="12dp">

    <TextView
        android:id="@+id/quoteText"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:textStyle="italic"
        android:textColor="#1A1A1A"
        android:textSize="15sp"
        android:ellipsize="end"
        tools:text="You do not rise to the level of your goals." />

    <ImageView
        android:id="@+id/quoteInk"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:scaleType="fitCenter"
        android:contentDescription="@string/app_name" />
</FrameLayout>
